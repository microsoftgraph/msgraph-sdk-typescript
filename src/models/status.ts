/** Provides operations to manage the admin singleton. */
export enum Status {
    Active = "active",
    Updated = "updated",
    Deleted = "deleted",
    Ignored = "ignored",
    UnknownFutureValue = "unknownFutureValue",
}
