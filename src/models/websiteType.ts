/** Provides operations to manage the collection of applicationTemplate entities. */
export enum WebsiteType {
    Other = "other",
    Home = "home",
    Work = "work",
    Blog = "blog",
    Profile = "profile",
}
