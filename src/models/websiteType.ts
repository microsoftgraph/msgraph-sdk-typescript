/** Provides operations to manage the collection of application entities. */
export enum WebsiteType {
    Other = "other",
    Home = "home",
    Work = "work",
    Blog = "blog",
    Profile = "profile",
}
