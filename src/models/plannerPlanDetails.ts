// tslint:disable
// eslint-disable
// Generated by Microsoft Kiota
import { deserializeIntoEntity, serializeEntity, type Entity } from './entity';
import { createPlannerCategoryDescriptionsFromDiscriminatorValue, serializePlannerCategoryDescriptions, type PlannerCategoryDescriptions } from './plannerCategoryDescriptions';
import { createPlannerUserIdsFromDiscriminatorValue, serializePlannerUserIds, type PlannerUserIds } from './plannerUserIds';
import { type Parsable, type ParseNode, type SerializationWriter } from '@microsoft/kiota-abstractions';

export function createPlannerPlanDetailsFromDiscriminatorValue(parseNode: ParseNode | undefined) {
    if(!parseNode) throw new Error("parseNode cannot be undefined");
    return deserializeIntoPlannerPlanDetails;
}
export function deserializeIntoPlannerPlanDetails(plannerPlanDetails: PlannerPlanDetails | undefined = {} as PlannerPlanDetails) : Record<string, (node: ParseNode) => void> {
    return {
        ...deserializeIntoEntity(plannerPlanDetails),
        "categoryDescriptions": n => { plannerPlanDetails.categoryDescriptions = n.getObjectValue<PlannerCategoryDescriptions>(createPlannerCategoryDescriptionsFromDiscriminatorValue); },
        "sharedWith": n => { plannerPlanDetails.sharedWith = n.getObjectValue<PlannerUserIds>(createPlannerUserIdsFromDiscriminatorValue); },
    }
}
export interface PlannerPlanDetails extends Entity, Parsable {
    /**
     * An object that specifies the descriptions of the 25 categories that can be associated with tasks in the plan.
     */
    categoryDescriptions?: PlannerCategoryDescriptions;
    /**
     * Set of user IDs that this plan is shared with. If you're using Microsoft 365 groups, use the Groups API to manage group membership to share the group's plan. You can also add existing members of the group to this collection, although it isn't required for them to access the plan owned by the group.
     */
    sharedWith?: PlannerUserIds;
}
export function serializePlannerPlanDetails(writer: SerializationWriter, plannerPlanDetails: PlannerPlanDetails | undefined = {} as PlannerPlanDetails) : void {
        serializeEntity(writer, plannerPlanDetails)
        writer.writeObjectValue<PlannerCategoryDescriptions>("categoryDescriptions", plannerPlanDetails.categoryDescriptions, );
        writer.writeObjectValue<PlannerUserIds>("sharedWith", plannerPlanDetails.sharedWith, );
}
// tslint:enable
// eslint-enable
