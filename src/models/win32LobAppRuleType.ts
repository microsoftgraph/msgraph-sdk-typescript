/**
 * Contains rule types for Win32 LOB apps.
 */
export enum Win32LobAppRuleType {
    /** Detection rule. */
    Detection = "detection",
    /** Requirement rule. */
    Requirement = "requirement",
}
