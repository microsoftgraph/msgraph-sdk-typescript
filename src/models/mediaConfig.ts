// tslint:disable
// eslint-disable
// Generated by Microsoft Kiota
import { deserializeIntoAppHostedMediaConfig } from './appHostedMediaConfig';
import { type AppHostedMediaConfig, type ServiceHostedMediaConfig } from './index';
import { deserializeIntoServiceHostedMediaConfig } from './serviceHostedMediaConfig';
import { type AdditionalDataHolder, type Parsable, type ParseNode, type SerializationWriter } from '@microsoft/kiota-abstractions';

export function createMediaConfigFromDiscriminatorValue(parseNode: ParseNode | undefined) {
    if(!parseNode) throw new Error("parseNode cannot be undefined");
    const mappingValueNode = parseNode.getChildNode("@odata.type");
    if (mappingValueNode) {
        const mappingValue = mappingValueNode.getStringValue();
        if (mappingValue) {
            switch (mappingValue) {
                case "#microsoft.graph.appHostedMediaConfig":
                    return deserializeIntoAppHostedMediaConfig;
                case "#microsoft.graph.serviceHostedMediaConfig":
                    return deserializeIntoServiceHostedMediaConfig;
            }
        }
    }
    return deserializeIntoMediaConfig;
}
export function deserializeIntoMediaConfig(mediaConfig: MediaConfig | undefined = {} as MediaConfig) : Record<string, (node: ParseNode) => void> {
    return {
        "@odata.type": n => { mediaConfig.odataType = n.getStringValue(); },
    }
}
export interface MediaConfig extends AdditionalDataHolder, Parsable {
    /**
     * Stores additional data not described in the OpenAPI description found when deserializing. Can be used for serialization as well.
     */
    additionalData?: Record<string, unknown>;
    /**
     * The OdataType property
     */
    odataType?: string;
}
export function serializeMediaConfig(writer: SerializationWriter, mediaConfig: MediaConfig | undefined = {} as MediaConfig) : void {
    writer.writeStringValue("@odata.type", mediaConfig.odataType);
    writer.writeAdditionalData(mediaConfig.additionalData);
}
// tslint:enable
// eslint-enable
