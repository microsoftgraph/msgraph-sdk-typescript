/** Casts the previous resource to user. */
export enum MeetingChatMode {
    Enabled = "enabled",
    Disabled = "disabled",
    Limited = "limited",
    UnknownFutureValue = "unknownFutureValue",
}
