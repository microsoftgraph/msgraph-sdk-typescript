// tslint:disable
// eslint-disable
// Generated by Microsoft Kiota
import { deserializeIntoEntity, serializeEntity, type Entity } from './entity';
import { createInternetExplorerModeFromDiscriminatorValue, serializeInternetExplorerMode, type InternetExplorerMode } from './internetExplorerMode';
import { type Parsable, type ParseNode, type SerializationWriter } from '@microsoft/kiota-abstractions';

export function createEdgeFromDiscriminatorValue(parseNode: ParseNode | undefined) {
    if(!parseNode) throw new Error("parseNode cannot be undefined");
    return deserializeIntoEdge;
}
export function deserializeIntoEdge(edge: Edge | undefined = {} as Edge) : Record<string, (node: ParseNode) => void> {
    return {
        ...deserializeIntoEntity(edge),
        "internetExplorerMode": n => { edge.internetExplorerMode = n.getObjectValue<InternetExplorerMode>(createInternetExplorerModeFromDiscriminatorValue); },
    }
}
export interface Edge extends Entity, Parsable {
    /**
     * A container for Internet Explorer mode resources.
     */
    internetExplorerMode?: InternetExplorerMode;
}
export function serializeEdge(writer: SerializationWriter, edge: Edge | undefined = {} as Edge) : void {
    serializeEntity(writer, edge)
    writer.writeObjectValue<InternetExplorerMode>("internetExplorerMode", edge.internetExplorerMode, serializeInternetExplorerMode);
}
// tslint:enable
// eslint-enable
