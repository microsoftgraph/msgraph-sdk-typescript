// tslint:disable
// eslint-disable
// Generated by Microsoft Kiota
import { deserializeIntoGroupAssignmentTarget, serializeGroupAssignmentTarget, type GroupAssignmentTarget } from './groupAssignmentTarget';
import { type Parsable, type ParseNode, type SerializationWriter } from '@microsoft/kiota-abstractions';

export function createExclusionGroupAssignmentTargetFromDiscriminatorValue(parseNode: ParseNode | undefined) {
    if(!parseNode) throw new Error("parseNode cannot be undefined");
    return deserializeIntoExclusionGroupAssignmentTarget;
}
export function deserializeIntoExclusionGroupAssignmentTarget(exclusionGroupAssignmentTarget: ExclusionGroupAssignmentTarget | undefined = {} as ExclusionGroupAssignmentTarget) : Record<string, (node: ParseNode) => void> {
    return {
        ...deserializeIntoGroupAssignmentTarget(exclusionGroupAssignmentTarget),
    }
}
export interface ExclusionGroupAssignmentTarget extends GroupAssignmentTarget, Parsable {
}
export function serializeExclusionGroupAssignmentTarget(writer: SerializationWriter, exclusionGroupAssignmentTarget: ExclusionGroupAssignmentTarget | undefined = {} as ExclusionGroupAssignmentTarget) : void {
        serializeGroupAssignmentTarget(writer, exclusionGroupAssignmentTarget)
}
// tslint:enable
// eslint-enable
