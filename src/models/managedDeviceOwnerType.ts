/** Provides operations to manage the deviceManagement singleton. */
export enum ManagedDeviceOwnerType {
    /** Unknown. */
    Unknown = "unknown",
    /** Owned by company. */
    Company = "company",
    /** Owned by person. */
    Personal = "personal",
}
