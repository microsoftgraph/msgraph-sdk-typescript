/** Provides operations to manage the admin singleton. */
export enum ManagedDeviceOwnerType {
    /** Unknown. */
    Unknown = "unknown",
    /** Owned by company. */
    Company = "company",
    /** Owned by person. */
    Personal = "personal",
}
