/** Provides operations to manage the collection of agreement entities. */
export enum RunAsAccountType {
    /** System context */
    System = "system",
    /** User context */
    User = "user",
}
