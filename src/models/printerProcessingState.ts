export enum PrinterProcessingState {
    Unknown = "unknown",
    Idle = "idle",
    Processing = "processing",
    Stopped = "stopped",
    UnknownFutureValue = "unknownFutureValue",
}
// tslint:enable
// eslint-enable
