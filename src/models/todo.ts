// tslint:disable
// eslint-disable
// Generated by Microsoft Kiota
import { deserializeIntoEntity, serializeEntity, type Entity } from './entity';
import { createTodoTaskListFromDiscriminatorValue, serializeTodoTaskList, type TodoTaskList } from './todoTaskList';
import { type Parsable, type ParseNode, type SerializationWriter } from '@microsoft/kiota-abstractions';

export function createTodoFromDiscriminatorValue(parseNode: ParseNode | undefined) {
    if(!parseNode) throw new Error("parseNode cannot be undefined");
    return deserializeIntoTodo;
}
export function deserializeIntoTodo(todo: Todo | undefined = {} as Todo) : Record<string, (node: ParseNode) => void> {
    return {
        ...deserializeIntoEntity(todo),
        "lists": n => { todo.lists = n.getCollectionOfObjectValues<TodoTaskList>(createTodoTaskListFromDiscriminatorValue); },
    }
}
export function serializeTodo(writer: SerializationWriter, todo: Todo | undefined = {} as Todo) : void {
        serializeEntity(writer, todo)
        writer.writeCollectionOfObjectValues<TodoTaskList>("lists", todo.lists, );
}
export interface Todo extends Entity, Parsable {
    /**
     * The task lists in the users mailbox.
     */
    lists?: TodoTaskList[];
}
// tslint:enable
// eslint-enable
